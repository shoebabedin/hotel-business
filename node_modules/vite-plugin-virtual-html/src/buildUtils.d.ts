import { VirtualHtmlPage } from './types';
/**
 * use pages' key as html name
 * @param pages
 */
export declare function extractHtmlPath(pages: {
    [p: string]: VirtualHtmlPage;
}): {
    [key: string]: string;
};
/**
 * get html file's name
 * @param id
 */
export declare function getHtmlName(id: string, root?: string): string;
/**
 * add trailing slash on path
 * @param {string} path
 * @returns {string}
 */
export declare function addTrailingSlash(path: string): string;
