import { InjectCode, Pages } from './types';
/**
 * find all html file in project and return it as Pages
 */
export declare function findAllHtmlInProject(extraGlobPattern?: Array<string>): Pages;
/**
 * directly use find\replacement / replacement\find to replace find
 * @param {pos, find, replacement}
 * @param code
 */
export declare function generateInjectCode({ pos, find, replacement }: InjectCode, code: string): string;
